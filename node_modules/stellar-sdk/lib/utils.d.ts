import { FeeBumpTransaction, Keypair, Transaction } from "stellar-base";
import { ServerApi } from "./server_api";
export declare namespace Utils {
    function buildChallengeTx(serverKeypair: Keypair, clientAccountID: string, homeDomain: string, timeout: number | undefined, networkPassphrase: string): string;
    function readChallengeTx(challengeTx: string, serverAccountID: string, networkPassphrase: string, _homeDomain?: string): {
        tx: Transaction;
        clientAccountID: string;
    };
    function verifyChallengeTxThreshold(challengeTx: string, serverAccountID: string, networkPassphrase: string, threshold: number, signerSummary: ServerApi.AccountRecordSigners[]): string[];
    function verifyChallengeTxSigners(challengeTx: string, serverAccountID: string, networkPassphrase: string, signers: string[]): string[];
    function verifyTxSignedBy(transaction: FeeBumpTransaction | Transaction, accountID: string): boolean;
    function gatherTxSigners(transaction: FeeBumpTransaction | Transaction, signers: string[]): string[];
}
